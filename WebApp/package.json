{
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "name": "webapp",
  "version": "1.0.0",
  "main": "./app/views/produtos",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "json-server --watch db/db.json --port 3001",
    "backfront": "json-server --watch db/db.json --port 3001 --static ./app/views/produtos"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
